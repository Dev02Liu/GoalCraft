<UserControl x:Class="GoalCraft.MVVM.View.NavBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GoalCraft.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Expander>
            <StackPanel Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center" Width="100" Height="200">
                <Button Content="Home" Command="{Binding NavigateCommand}" CommandParameter="Home" />
                <Button Content="Create" Command="{Binding NavigateCommand}" CommandParameter="Create" />
                <Button Content="Manage" Command="{Binding NavigateCommand}" CommandParameter="Manage" />
            </StackPanel>
        </Expander>
    </Grid>
</UserControl>
